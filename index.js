/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { SuperHeroApp } from './src/SuperHeroApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => SuperHeroApp);
